Infinite - Third-Party Notices
===============================

This file lists third-party components bundled with Infinite beyond what
`LICENSE` already covers (the Steinberg VST3 SDK and, on Windows, ONNX
Runtime / DirectML / u2netp.onnx - see `LICENSE` and
`assets/models/NOTICE.txt`). It will grow; this is the first entry, added
alongside Linux Phase 3 (video/recorder/camera).

FFmpeg (Linux)
--------------

Used for video decode, the movie recorder's H.264 video / AAC audio
encoding, and video-container audio-track decode
(`src/platform/linux/MediaLinux.cpp`), linked as prebuilt shared libraries
(`libavformat`, `libavcodec`, `libavutil`, `libswscale`, `libswresample`).

Prebuilt binaries (what actually links into the shipped binary), from the
BtbN/FFmpeg-Builds autobuild tag `autobuild-2026-09-15-13-18`, whose asset
filenames embed the version string `n8.1.2-53-g1005b294ff` (a git snapshot 53
commits past the `n8.1.2` release, not the `8.1` release tarball):

| Variant | Arch | Archive - SHA256 |
|---|---|---|
| `INFINITE_ENABLE_GPL_CODECS=ON` (default) | x86_64 | `ffmpeg-n8.1.2-53-g1005b294ff-linux64-gpl-shared-8.1.tar.xz` - `cdd23dda4552a11babd04b1e352736339d50cb2bca29f9fd2014e246a3924062` |
| `INFINITE_ENABLE_GPL_CODECS=ON` (default) | aarch64 | `ffmpeg-n8.1.2-53-g1005b294ff-linuxarm64-gpl-shared-8.1.tar.xz` - `9b1d14ac6ce89d35ae2511f150477680931a482ea53fd033850f3d5d27af0141` |
| `INFINITE_ENABLE_GPL_CODECS=OFF` | x86_64 | `ffmpeg-n8.1.2-53-g1005b294ff-linux64-lgpl-shared-8.1.tar.xz` - `c8fb071899337b16680ea1848c87fac27c3f31ff2c88e3c3b6c813f2625e9af1` |
| `INFINITE_ENABLE_GPL_CODECS=OFF` | aarch64 | `ffmpeg-n8.1.2-53-g1005b294ff-linuxarm64-lgpl-shared-8.1.tar.xz` - `aac9ed865431ce338688d657ece3345712107a891dee77d6cad5f6d2fcf58ec5` |

Downloaded from
`https://github.com/BtbN/FFmpeg-Builds/releases/download/autobuild-2026-09-15-13-18/<name>`.
See `CMakeLists.txt` (search `FFMPEG_RELEASE_TAG`) for the
`FetchContent_Declare` that pins these, and the comment above it for why the
pin is an immutable `autobuild-*` tag rather than the rolling `latest` tag
(whose assets are replaced in place, so a hash pinned against it goes stale).

**Source offer (GPL section 6).** BtbN distributes prebuilt binaries only;
the obligation to make corresponding source available falls on this project
as the distributor of the built binary, not on BtbN. The corresponding source
for the binaries above is:

- FFmpeg at git commit `1005b294ff` on the `release/8.1` branch - the commit
  the `n8.1.2-53-g1005b294ff` version string names. Snapshot:
  `https://git.ffmpeg.org/gitweb/ffmpeg.git/snapshot/1005b294ff.tar.gz`
  (equivalently `https://github.com/FFmpeg/FFmpeg/archive/1005b294ff.tar.gz`).
  This is NOT the `ffmpeg-8.1.tar.xz` release tarball; a snapshot 53 commits
  later is a different work and only the actual commit satisfies section 6.
- x264 at git commit `0480cb05fa188d37ae87e8f4fd8f1aea3711f7ee`. Not published
  in the release metadata or embedded as a readable string in the shipped
  `libavcodec.so` (x264's git-describe string is only baked in when built
  from a live git checkout with a `.git` dir present, which BtbN's Docker
  build does not preserve). Resolved instead from BtbN's own build recipe:
  `scripts.d/50-x264.sh` in `BtbN/FFmpeg-Builds` pins `SCRIPT_COMMIT` to this
  hash, cloned from `https://code.videolan.org/videolan/x264.git`, and that
  pin was last changed 2025-10-24 - before this autobuild (2026-09-15), so it
  was still in effect. Source:
  `https://code.videolan.org/videolan/x264/-/archive/0480cb05fa188d37ae87e8f4fd8f1aea3711f7ee/x264-0480cb05fa188d37ae87e8f4fd8f1aea3711f7ee.tar.gz`

Anyone who receives the Linux binary of Infinite may request this exact
corresponding source under GPLv2-or-later/GPLv3 section 6. The written offer
is: open an issue at the project's repository.

**Verified source archives (release assets).** Both tarballs above have been
downloaded and hashed, and ship as GitHub release assets on this release
(`ffmpeg-1005b294ff-src.tar.gz`, `x264-0480cb05-src.tar.gz`), so fulfilment
does not depend on upstream URLs staying up:

| Archive | SHA256 |
|---|---|
| `ffmpeg-1005b294ff-src.tar.gz` (FFmpeg source, commit `1005b294ff`) | `acd4385e9084beff6497030fc832a920e1699b87a79c80a30e787c2d3469ecfa` |
| `x264-0480cb05-src.tar.gz` (x264 source, commit `0480cb05fa18`) | `d0967a1348c85dfde363bb52610403be898171493100561efa0dd05d5fd1ae50` |

**Licence texts shipped with the Linux build.** `LICENSE.txt` inside each
BtbN FFmpeg release tarball is the GPLv3 text (the FFmpeg build's own
declared licence given `--enable-gpl`). This repository additionally vendors,
under `third_party/`:

- `third_party/ffmpeg/COPYING.GPLv3` - FFmpeg's own GPLv3 text
- `third_party/ffmpeg/COPYING.GPLv2` - FFmpeg's own GPLv2 text (applies to
  the `INFINITE_ENABLE_GPL_CODECS=OFF` / LGPL build's optional GPL parts,
  none of which are enabled in that configuration, kept for completeness)
- `third_party/ffmpeg/COPYING.LGPLv2.1` - the licence that applies when
  `INFINITE_ENABLE_GPL_CODECS=OFF` (LGPL build, no x264, no `--enable-gpl`)
- `third_party/x264/COPYING` - x264's GPLv2-or-later licence text

CMake copies these next to the installed/bundled binary on Linux (see the
`Copying FFmpeg/x264 third-party licence texts` custom command in
`CMakeLists.txt`), the same way `assets/models/NOTICE.txt` accompanies
`u2netp.onnx` on Windows.

x264 (Linux, `INFINITE_ENABLE_GPL_CODECS=ON` only)
---------------------------------------------------

The H.264 encoder FFmpeg's `libavcodec` uses for `Platform::RecorderStart`'s
video track when GPL codecs are enabled. Not linked directly by Infinite -
it is compiled into the BtbN FFmpeg build's `libavcodec.so` - but it is the
component that makes the resulting binary GPL rather than merely
LGPL-encumbered, and the reason the Linux build is a first-time H.264
encoder *distributor* for this project (see `LICENSE` and
`docs/plans/linux/phase-03-media.md`).

Licence: GNU General Public License v2 or later. Source pinned above
alongside FFmpeg's. Licence text: `third_party/x264/COPYING`.

VA-API hardware encode
-----------------------

Not used. Considered as a way to move Linux's H.264 encoding burden onto
GPU-vendor-licensed silicon (matching macOS's VideoToolbox and Windows'
Media Foundation hardware paths) but explicitly scoped out of Phase 3 - it
cannot be verified under the llvmpipe container or in CI, and is deferred to
a phase with real hardware. libx264 software encode is the only Linux H.264
path today.

Signalsmith Stretch (all platforms)
-----------------------------------

Time-stretch / pitch-shift for Arrangement Timeline audio Samples
(`src/audio/dsp/ClipTimeStretch.h`). Header-only, vendored in
`external/signalsmith-stretch/`.

| Component | Version | Commit |
|---|---|---|
| signalsmith-stretch | 1.3.2 | `57b93f4e9206a089a45387eaa39bdc9f310d3308` |
| signalsmith-linear | 0.3.1 | `5668673560146a9cfe38c25315071e3fd68c8317` |

Local modifications (marked `INFINITE PATCH`):
- `signalsmith-stretch.h`: the silent-input bypass also waits until the
  output latency's worth of silence has been produced, so synthesis tails are
  not cut off at stretch rates above about 2x.
- `signalsmith-linear/fft.h`: adds the missing `#include <cstring>`
  (`std::memcpy`); it built only through libc++'s transitive includes and
  fails on libstdc++ (Linux) and MSVC.

FFT backend: Apple's Accelerate on macOS; the library's built-in portable
C++ FFT on Windows and Linux (no intrinsics, so x64 and ARM64 alike).
Measured at the same cost and bit-identical output to the Accelerate path.

Licence: MIT. Licence texts: `external/signalsmith-stretch/LICENSE.txt` and
`external/signalsmith-stretch/signalsmith-linear/LICENSE.txt`.
