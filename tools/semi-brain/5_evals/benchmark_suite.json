[
  {
    "id": "CASE_01",
    "query": "Fix audio pop and click when retriggering an arranged audio clip mid-playback with active envelope modulation",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["ParamMailbox latency <= 1 block", "Zero heap allocation in audio thread", "Phase continuity on retrigger"],
    "target_symbols": ["AudioEngine", "ParamMailbox", "Clip"]
  },
  {
    "id": "CASE_02",
    "query": "ArrangeSliderFloat text-edit race where double clicking to enter value gets focus stolen by canvas click handler",
    "expected_subsystem": "arrange_timeline",
    "required_invariants": ["Text-edit focus isolation", "Monotonic UID stability"],
    "target_symbols": ["ArrangeSliderFloat", "ArrangeModel"]
  },
  {
    "id": "CASE_03",
    "query": "Field language parser allows bare names but crashes when assigning to element domain builtin P.y",
    "expected_subsystem": "field_compiler",
    "required_invariants": ["Zero-sigil bare name syntax", "Domain rate type inference soundness"],
    "target_symbols": ["Expression", "FieldCompiler"]
  },
  {
    "id": "CASE_04",
    "query": "Render3D node offline video export drops frames because scene cache is not forcibly invalidated each tick",
    "expected_subsystem": "render_3d",
    "required_invariants": ["Offline scene cache redraw", "Transport deterministic clock sync"],
    "target_symbols": ["Render3DNode", "IGeometrySource"]
  },
  {
    "id": "CASE_05",
    "query": "Audio Color Ramp node input pin disappeared because InputCountFor was hardcoded to 1 instead of dynamic",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["Pin wiring chain registration", "Idempotent Value01 contract"],
    "target_symbols": ["AudioColorRampNode", "INode"]
  },
  {
    "id": "CASE_06",
    "query": "Duplicate or split audio clip in timeline reuses existing UID causing undo/redo collision",
    "expected_subsystem": "arrange_timeline",
    "required_invariants": ["Monotonic UID allocation", "Save/load roundtrip identity bit-for-bit"],
    "target_symbols": ["Clip", "ArrangeModel"]
  },
  {
    "id": "CASE_07",
    "query": "Prevent shader recompile every frame in FilterDef compositing node when parameters change",
    "expected_subsystem": "compositing_2d",
    "required_invariants": ["Uniform param updates without recompile", "Cook memoization under fan-out"],
    "target_symbols": ["FilterDef", "GLUtil"]
  },
  {
    "id": "CASE_08",
    "query": "VST3 plugin rescan freezes application dock icon on macOS during background bundle scan",
    "expected_subsystem": "platform",
    "required_invariants": ["Non-blocking async thread scan", "Cross-platform facade isolation"],
    "target_symbols": ["Platform", "PluginVST3"]
  },
  {
    "id": "CASE_09",
    "query": "Wavetable synth oscillator clicks on note-off due to missing release phase envelope smoothing",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["Exponential decay release", "Two-object rule INode vs DSP worker"],
    "target_symbols": ["WavetableNode", "AudioEngine"]
  },
  {
    "id": "CASE_10",
    "query": "Note Stack semitone offsets fail sweep test because mailbox delivery was not called before audio render",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["ParamMailbox delivery within 1 block", "AUDIOPARAMSWEEPTEST conformance"],
    "target_symbols": ["NoteStackNode", "ParamMailbox"]
  },
  {
    "id": "CASE_11",
    "query": "Canvas drop handler steals Finder file drops meant for the timeline arrangement lanes",
    "expected_subsystem": "arrange_timeline",
    "required_invariants": ["Z-order hit test precedence", "Drop target routing"],
    "target_symbols": ["ArrangePanel", "DropManager"]
  },
  {
    "id": "CASE_12",
    "query": "Field compiler domain rate error when adding per-pixel bass audio FFT magnitude directly inside vertex kernel",
    "expected_subsystem": "field_compiler",
    "required_invariants": ["Explicit reduce/resample domain transfer", "Domain hierarchy ordering"],
    "target_symbols": ["FieldCompiler", "DomainTransfer"]
  },
  {
    "id": "CASE_13",
    "query": "Bypassing Curves node alters texture resolution and leaves distortion active on downstream output",
    "expected_subsystem": "compositing_2d",
    "required_invariants": ["BypassSource pass-through bit-for-bit", "Resolution ownership preservation"],
    "target_symbols": ["CurvesNode", "INode"]
  },
  {
    "id": "CASE_14",
    "query": "Metaball mesh operator weld defect on x86_64 Linux due to unaligned SIMD vector instructions",
    "expected_subsystem": "render_3d",
    "required_invariants": ["SIMD memory alignment", "Cross-platform parity macOS/Win/Linux"],
    "target_symbols": ["MetaballNode", "Platform"]
  },
  {
    "id": "CASE_15",
    "query": "Timeline playhead scrubs with noticeable FPS drop due to synchronous disk file reads during waveform draw",
    "expected_subsystem": "arrange_timeline",
    "required_invariants": ["Live-drawn cached waveform bounds", "Non-blocking UI thread rendering"],
    "target_symbols": ["ArrangeModel", "WaveformCache"]
  },
  {
    "id": "CASE_16",
    "query": "Deleting audio delay node mid-playback causes crash due to dangling modulation binding",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["AUDIOTEARDOWNSWEEPTEST safety", "Atomic unbinding on delete"],
    "target_symbols": ["DelayNode", "ModulationMatrix"]
  },
  {
    "id": "CASE_17",
    "query": "Keyboard shortcuts help window lists Ctrl+S but handler was gated exclusively on Cmd on Windows",
    "expected_subsystem": "ui_shell",
    "required_invariants": ["Cmd/Ctrl unified platform modifier", "Shortcuts sweep verification"],
    "target_symbols": ["ShortcutsWindow", "InputManager"]
  },
  {
    "id": "CASE_18",
    "query": "Feedback node persistent state ping-pong texture corrupts colors when canvas resolution is resized",
    "expected_subsystem": "compositing_2d",
    "required_invariants": ["State buffer reallocation on resize", "Ping-pong texture clean reset"],
    "target_symbols": ["FeedbackNode", "GLUtil"]
  },
  {
    "id": "CASE_19",
    "query": "Audio device unplug on Windows causes audio thread deadlock in WASAPI release handler",
    "expected_subsystem": "platform",
    "required_invariants": ["WASAPI device loss recovery", "Lock-free audio shutdown"],
    "target_symbols": ["Platform", "AudioEngine"]
  },
  {
    "id": "CASE_20",
    "query": "Curves node spline handles do not save correctly into patch JSON grammar causing flat line on reload",
    "expected_subsystem": "compositing_2d",
    "required_invariants": ["Save/load patch roundtrip identity", "Token serialization grammar"],
    "target_symbols": ["CurvesNode", "Patch"]
  },
  {
    "id": "CASE_21",
    "query": "Quantize note filter node forces C Major but detune knob running after overrides scale",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["Invariant interaction audit (Sibling-Undo rule)", "Rhythmic and pitch quantization standard"],
    "target_symbols": ["NoteFilterNode", "MusicTime"]
  },
  {
    "id": "CASE_22",
    "query": "Field Element kernel memory leak from allocating std::vector per vertex inside compute loop",
    "expected_subsystem": "field_compiler",
    "required_invariants": ["Zero heap allocation in element domain", "Pre-allocated flat arrays"],
    "target_symbols": ["FieldElementNode", "VM"]
  },
  {
    "id": "CASE_23",
    "query": "Track groups in Timeline arrange panel collapse but lane heights do not update causing visual overlap",
    "expected_subsystem": "arrange_timeline",
    "required_invariants": ["Collapsible hierarchy layout discipline", "Track group unique naming"],
    "target_symbols": ["ArrangePanel", "ArrangeModel"]
  },
  {
    "id": "CASE_24",
    "query": "Spout texture sender on Windows crashes when receiver disconnects abruptly",
    "expected_subsystem": "platform",
    "required_invariants": ["DirectX shared handle validation", "Clean teardown without GL context corruption"],
    "target_symbols": ["SpoutOutNode", "Platform"]
  },
  {
    "id": "CASE_25",
    "query": "LFO modulator Value01 returns different values when polled by 3 separate destination cables in one frame",
    "expected_subsystem": "nodes",
    "required_invariants": ["IModulator Value01 idempotency within single frame", "Phase clock tick synchronization"],
    "target_symbols": ["LFONode", "IModulator"]
  },
  {
    "id": "CASE_26",
    "query": "Render3D mapping node UV scale parameter changes do not propagate downstream to mesh render",
    "expected_subsystem": "render_3d",
    "required_invariants": ["Revision dirty stamp propagation", "IGeometrySource transform contract"],
    "target_symbols": ["MappingNode", "Render3DNode"]
  },
  {
    "id": "CASE_27",
    "query": "Syphon output on macOS flips texture vertically when publishing to external VJ software",
    "expected_subsystem": "platform",
    "required_invariants": ["OpenGL framebuffer coordinate origin", "Output projection sweep"],
    "target_symbols": ["SyphonOutNode", "Platform"]
  },
  {
    "id": "CASE_28",
    "query": "Reverb node freeze button causes infinite audio feedback gain runaway exceeding 0dB",
    "expected_subsystem": "audio_dsp",
    "required_invariants": ["DSP safety limiter clamp [-1.0, 1.0]", "Two-object rule parameter delivery"],
    "target_symbols": ["ReverbNode", "AudioEngine"]
  },
  {
    "id": "CASE_29",
    "query": "Export movie audio and video drift out of sync after 2 minutes of recording",
    "expected_subsystem": "core_system",
    "required_invariants": ["Audio sample counter as master transport clock", "PBO readback sync sweep"],
    "target_symbols": ["OutputNode", "Recorder"]
  },
  {
    "id": "CASE_30",
    "query": "Field state cell does not reset phase when transport stops and loops back to bar 1",
    "expected_subsystem": "field_compiler",
    "required_invariants": ["State cell reset on transport loop/seek", "Delay sugar semantics"],
    "target_symbols": ["FieldState", "Transport"]
  }
]
